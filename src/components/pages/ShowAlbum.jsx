import React from 'react';

const ShowAlbum = (props) => {
    console.log(props)
    return (
        <div className="card">
            <img src={url} alt="photo"/>
        </div>
    );
};

export default ShowAlbum;
